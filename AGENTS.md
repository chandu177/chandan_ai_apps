## Databricks

By default, when using the Databricks CLI, always use the --profile `default`

## Dev Workflow

- Check out latest from main
- Create a new git feature branch and give it a fitting name
- Create a new Lakebase branch and give it a fitting name
- Point the app locally against the Lakebase branch
- Make the changes 
- Running app locally for review